[project]
name = "maze-generator"
version = "0.1.0"
description = "Maze generator"
readme = "README.md"
authors = [{ name = "Vincent Poulailleau", email = "vpoulailleau@gmail.com" }]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
maze-generator = "maze_generator:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
